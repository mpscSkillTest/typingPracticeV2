/**
 * Enum for user type
 * @readonly
 * @enum {{name: string, value: string}}
 */
const UserType = Object.freeze({
  STUDENT: { name: "STUDENT", value: "Student" },
  INSTITUTE: { name: "INSTITUTE", value: "Institute" },
});

export default UserType;
